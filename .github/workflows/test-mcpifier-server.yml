name: Test test-mcpifier-server

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'src/Summerdawn.Mcpifier.Server/**'
      - 'tests/Summerdawn.Mcpifier.Server.Tests/**'
      # Project references
      - 'src/Summerdawn.Mcpifier/**'
      - 'src/Summerdawn.Mcpifier.AspNetCore/**'

permissions:
  contents: read
  actions: read
  checks: write

env:
  DOTNET_VERSION: '8.0.x'

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        
      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
          
      - name: Restore dependencies
        run: dotnet restore tests/Summerdawn.Mcpifier.Server.Tests/Summerdawn.Mcpifier.Server.Tests.csproj
        
      - name: Build
        run: dotnet build tests/Summerdawn.Mcpifier.Server.Tests/Summerdawn.Mcpifier.Server.Tests.csproj --no-restore --configuration Release
        
      - name: Run tests
        run: dotnet test tests/Summerdawn.Mcpifier.Server.Tests/Summerdawn.Mcpifier.Server.Tests.csproj --no-build --configuration Release --verbosity normal --logger "trx;LogFileName=test-results.trx"
        
      - name: Upload test results
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: test-results
          path: '**/test-results.trx'

      - name: Publish test results
        uses: dorny/test-reporter@v3
        if: always()
        with:
          name: test-results
          path: '**/test-results.trx'
          reporter: dotnet-trx
          fail-on-error: false
          token: ${{ secrets.GITHUB_TOKEN }}
